[package]
name = "host-lifecycle"
version = "0.9.1"
edition = "2021"
description = "Token-free lifecycle tool for an agentic project — scaffolds milestones, decisions, and personas, and validates the tree."
license = "Unlicense"

[dependencies]
host-grammar = { git = "https://github.com/connollydavid/host-grammar" }
host-lint = { git = "https://github.com/connollydavid/host-lint" }
