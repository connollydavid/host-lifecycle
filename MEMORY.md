- **A rename is migrated by the tool, not by the adopter (2026-07-23).** `host-lifecycle migrate-recipe <dir>` renames retired `.host-software` keys and drops keys no release ever read, idempotently, from one table; both ledger entries for [[call/0046]] and [[call/0047]] call it instead of instructing a hand edit, because a hand edit is where a weak agent renames the wrong line and a busy operator renames none. **Also worth keeping: the naming probe spoiled on this verb.** Four rotations produced two position artefacts (A twice, then C twice) rather than a preference, so the probe was declared spoiled and the name settled on the family it joins (`migrate-receipts`). A spoiled probe is data about the protocol; reporting a position artefact as a result would have been the dishonest move.
